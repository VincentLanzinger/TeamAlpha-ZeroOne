# Sybilion API credential.
# Canonical name per the official docs (Authorization: Bearer $SYBILION_API_TOKEN).
SYBILION_API_TOKEN=sk_ops_49b58d9a0ca1171fe81c1d302f882bd97fa6d186

# The client also accepts SYBILION_API_KEY as a fallback in case you already use that name.
# SYBILION_API_KEY=sk_ops_replace_me

# Featherless.ai key for the LLM narrator (Phase 7). Optional — without it,
# the narrator falls back to a deterministic template that produces the same
# structured output without a network call.
# FEATHERLESS_API_KEY=
# FEATHERLESS_MODEL=meta-llama/Meta-Llama-3.1-8B-Instruct
